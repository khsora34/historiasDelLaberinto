enum InitialScene {
    // MARK: Use cases
    
    enum LoadFiles {
        struct Response {
            let stringResponse: String
        }
    }
    enum RoomBuilder {
        struct Response {
            let room: Room?
        }
    }
}

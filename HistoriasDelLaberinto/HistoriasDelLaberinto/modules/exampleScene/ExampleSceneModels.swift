enum ExampleScene {
    // MARK: Use cases
    
    enum Something {
        struct Request {
            let input: String?
        }
        struct Response {
            let output: String?
        }
        struct ViewModel {
            let modeledValue: String?
        }
    }
}

struct ExampleScenePresenterInput {
    
}
